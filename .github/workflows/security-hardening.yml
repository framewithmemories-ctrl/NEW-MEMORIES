name: Security Hardening (manual)

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Security hardening is applied by backend/hardened.py and backend/Dockerfile."
